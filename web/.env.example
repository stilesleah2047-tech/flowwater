# AquaFlow now runs entirely as one Next.js app (frontend + API routes),
# deployed as a single Vercel project. Every variable below is needed —
# set them all in Vercel's Project Settings -> Environment Variables.

# ---- MongoDB ----
MONGODB_URI=mongodb+srv://user:password@cluster0.mongodb.net/aquaflow?retryWrites=true&w=majority

# ---- Auth / JWT ----
JWT_ACCESS_SECRET=replace_with_a_long_random_string
JWT_REFRESH_SECRET=replace_with_a_different_long_random_string
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=30d
DELIVERY_MAX_DEVICES=2

# ---- Safaricom Daraja ----
DARAJA_ENV=sandbox
DARAJA_CONSUMER_KEY=xxxxxxxxxxxxxxxx
DARAJA_CONSUMER_SECRET=xxxxxxxxxxxxxxxx
DARAJA_SHORTCODE=174379
DARAJA_PASSKEY=bfb279f9aa9bdbcf158e97dd71a467cd2e0c893059b10f78e6b72ada1ed2c919
DARAJA_TRANSACTION_TYPE=CustomerPayBillOnline
# Must be this deployment's own public URL + /api/mpesa/callback, set
# AFTER your first deploy once you know the real domain, e.g.
# https://your-app.vercel.app/api/mpesa/callback
DARAJA_CALLBACK_URL=https://your-app.vercel.app/api/mpesa/callback
DARAJA_CALLBACK_IP_ALLOWLIST=

# ---- Seed / bootstrap ----
SEED_SUPER_ADMIN_EMAIL=admin@aquaflow.local
SEED_SUPER_ADMIN_PASSWORD=change_me_immediately
